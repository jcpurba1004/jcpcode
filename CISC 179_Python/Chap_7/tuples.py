#image = Image("smokey.gif")
#(r, g, b) = image.getPixel(0, 0)

#r
#g
#b

#image.setPixel(0, 0, (r + 10, g + 10, b + 10))

def average(triple):
    (a, b, c) = triple
    return (a + b + c) // 3
average((40, 50, 60))
